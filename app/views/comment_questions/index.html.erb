<h1>Listing comment_questions</h1>

<table>
  <tr>
    <th>Body</th>
    <th>Question is</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @comment_questions.each do |comment_question| %>
  <tr>
    <td><%= comment_question.body %></td>
    <td><%= comment_question.question_is %></td>
    <td><%= comment_question.user_id %></td>
    <td><%= link_to 'Show', question_comment_question_path %></td>
    <td><%= link_to 'Edit', edit_question_comment_question_path(comment_question) %></td>
    <td><%= link_to 'Destroy', question_comment_question, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Comment question', new_question_comment_question_path %>
